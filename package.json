{
  "name": "setup-badger",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "prepare-agent": "ncc build agent/index.js -o agent/dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "keywords": [],
  "homepage": "https://github.com/fianulabs/actions",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fianulabs/actions.git"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/tool-cache": "^1.6.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^7.4.0",
    "jest": "^29.7.0"
  }
}
