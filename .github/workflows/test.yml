name: Tests

on:
  push:
    branches:
    - main
    - testing
  pull_request:

defaults:
  run:
    shell: bash

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, darwin-latest]
        version: [1.3.0]
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Setup gh
      uses: ./
      with:
        version: ${{ matrix.version }}

    - name: Test CLI
      run: |-
        fianu

#    - name: Capture gh version installedd
#      run: |
#        export GH_VERSION=$( gh --version )
#        echo 'GH_VERSION_INSTALLED<<EOF' >> $GITHUB_ENV
#        gh --version >> $GITHUB_ENV
#        echo 'EOF' >> $GITHUB_ENV

#    - name: Verify
#      shell: python
#      env:
#        GH_VERSION_EXPECTED: ${{ matrix.version }}
#      run: |
#        import sys, os
#        sys.exit(
#            int(not os.environ["GH_VERSION_EXPECTED"] in os.environ["GH_VERSION_INSTALLED"])
#        )
